void addLeafNodes(Node root, List<Integer> list) {
    if (root == null) return;

    if (root.left == null && root.right == null) {
        list.add(root.data);
        return;
    }

    addLeafNodes(root.left, list);
    addLeafNodes(root.right, list);
}
